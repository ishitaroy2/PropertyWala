import { styled, keyframes } from "@mui/system";

export const GradientBackground = styled("div")({
  background: "radial-gradient(circle at top right, #c3bbc3, #64956e ,#00d4ff)",
  minHeight: "100vh",
  display: "flex",
  flexDirection: "column",
  overflow: "hidden",
});
